[V]建立資料庫連線
	EF core 反向工程
	appsettings.json
		"ConnectionStrings": {
			"DefaultConnection": "Server=localhost;Database=TodoList;Member Id=sa;Password=your_password;"
		}
	在program.cs 註冊DbContext 連線字串
	  

[V]【後台會員管理實作】
    [V]1. 建立 MemberDto
        位置：ISpanShop.Models專案 -> DTOs 資料夾 -> MemberDto.cs
        -資料存取層 (ISpanShop.Repositories)
        目的：直接與資料庫對話，負責寫 LINQ 指令。

     [V]2. 建立介面 IMemberRepository
        位置：ISpanShop.Repositories專案 -> Interfaces 資料夾 -> IMemberRepository.cs
        說明：定義「搜尋使用者」的方法合約。

     [V]3. 實作 MemberRepository
        位置：ISpanShop.Repositories專案 -> (根目錄) -> MemberRepository.cs
        說明：實作介面，使用 EF Core 進行查詢。
        - 商務邏輯層 (ISpanShop.Services)
        目的：處理資料轉換 (Entity -> DTO) 與商業規則。

     [V]4. 建立介面 IMemberService
        位置：ISpanShop.Services專案 -> Interfaces 資料夾 -> IMemberService.cs
        說明：定義給 MVC Controller 呼叫的方法。

     [V]5. 實作 MemberService
        位置：ISpanShop.Services專案 -> (根目錄) -> MemberService.cs
        說明：注入 Repository，取得資料後轉換為 DTO。
        -依賴注入設定 (ISpanShop.MVC)
        目的：將上述寫好的 Service 與 Repository 註冊到容器中，讓系統知道 Interface 對應哪個 Class。

     [V]6. 註冊服務
        位置：ISpanShop.MVC專案 -> Program.cs
        動作：在 var app = builder.Build(); 這一行之前加入。
        // 註冊 Repositories註冊 Services
        builder.Services.AddScoped<IMemberRepository, MemberRepository>();
        builder.Services.AddScoped<IMemberService, MemberService>();
         5: 展示層 (ISpanShop.MVC)
        目的：負責接收請求、組裝 ViewModel 並呈現網頁。

     [V]7. 建立 ViewModel (MemberIndexVm)
        位置：ISpanShop.MVC專案 -> Models -> Members 資料夾 (需新增) -> MemberIndexVm.cs
        說明：專門給 View 顯示用的模型 (可能包含顯示格式化)。

     [V]8. 建立 Controller (MembersController)
        位置：ISpanShop.MVC專案 -> Controllers 資料夾 -> MembersController.cs
        說明：注入 Service，建立 Index Action。
     
     [V]9. 建立 View (Index.cshtml)
        位置：ISpanShop.MVC專案 -> Views -> Members 資料夾 (需新增) -> Index.cshtml
        說明：繪製表格。

[V]【後台會員管理實作】

    1: 定義資料傳輸物件 (ISpanShop.Models)
        目的：對應 Admins 資料表與權限顯示
        [ V] 建立 AdminsDto 與 AdminsRoleDto
        - 位置：ISpanShop.Models/DTOs/AdminsDtos.cs
        - 內容：AdminsDto (AdminId, Account, Email, RoleId, RoleName)
        - 內容：AdminsRoleDto (RoleId, RoleName) - 用於下拉選單

  
    2：資料存取層 (ISpanShop.Repositories)
        目的：直接操作 Admins 資料表 (ADO.NET)
        [V] 建立介面 IAdminRepository
            - 位置：ISpanShop.Repositories/Interfaces/IAdminRepository.cs
            - 方法：
                IEnumerable<AdminDto> GetAllAdmins()
                bool UpdateAdminRole(int adminId, int roleId)
        [V] 實作 AdminRepository
            - 位置：ISpanShop.Repositories/AdminRepository.cs
            - SQL GetAllAdmins：
                SELECT A.Id AS AdminId, A.Account, A.Email,
                       A.RoleId, R.RoleName
                FROM Admins A
                JOIN Roles R ON A.RoleId = R.Id
            - SQL UpdateAdminRole：
                UPDATE Admins SET RoleId = @RoleId WHERE Id = @AdminId
        [V] 建立介面 IAdminRoleRepository（若角色列表需獨立查詢）
            - 位置：ISpanShop.Repositories/Interfaces/IAdminRoleRepository.cs
            - 方法：
                IEnumerable<AdminRoleDto> GetAllRoles()
        [V] 實作 AdminRoleRepository
            - 位置：ISpanShop.Repositories/AdminRoleRepository.cs
            - SQL GetAllRoles：
                SELECT Id AS RoleId, RoleName FROM Roles


      3：商務邏輯層 (ISpanShop.Services)
        目的：處理管理員相關業務邏輯
        [ V] 建立介面 IAdminService
            - 位置：ISpanShop.Services/Interfaces/IAdminService.cs
            - 方法：
                IEnumerable<AdminDto> GetAllAdmins() 
                IEnumerable<AdminRoleDto> GetAllRoles()
                bool UpdateAdminRole(int adminId, int roleId, int currentAdminId)
        [ V   ] 實作 AdminService 
            - 位置：ISpanShop.Services/AdminService.cs
            - 邏輯：
                UpdateAdminRole 執行前，檢查
                adminId != currentAdminId
                （Self-Lockout Prevention：禁止管理員修改自己的權限）
                若相同則 throw 或回傳失敗訊息

        4：依賴注入設定 (ISpanShop.MVC)
        [v ] 在 Program.cs 註冊 Admin 相關服務
            - 位置：ISpanShop.MVC/Program.cs
            - 新增：
                builder.Services.AddScoped<IAdminRepository, AdminRepository>();
                builder.Services.AddScoped<IAdminRoleRepository, AdminRoleRepository>();
                builder.Services.AddScoped<IAdminService, AdminService>();

        5：展示層 (ISpanShop.MVC)
        目的：超級管理員專用的管理介面
        [v] 建立 ViewModel AdminIndexVm
            - 位置：ISpanShop.MVC/Models/Admins/AdminIndexVm.cs
            - 內容：
                List<AdminDto>     Admins       // 管理員列表
                List<AdminRoleDto> RoleOptions  // 角色下拉選單來源
                string             Message      // 操作結果訊息（成功/失敗）

        [V] 建立 AdminsController
            - 位置：ISpanShop.MVC/Controllers/AdminsController.cs
            - 權限：[Authorize(Roles = "SuperAdmin")]
            - Actions：
                GET  Index()
                     -> 取得所有管理員列表 + 角色選單，回傳 AdminIndexVm
                POST UpdateRole(int adminId, int roleId)
                     -> 呼叫 AdminService.UpdateAdminRole()
                     -> 操作完成後 RedirectToAction("Index")

        [V] 建立 View Index.cshtml
            - 位置：ISpanShop.MVC/Views/Admins/Index.cshtml
            - 功能：
                - 表格列出所有管理員（帳號、Email、目前角色）
                - 每列包含 <select> 下拉選單（來源：RoleOptions）
                - 每列包含「更新」按鈕，POST 至 UpdateRole
                - 頁面頂端顯示 Message（操作結果提示）
               // - 自己那列的 <select> 與按鈕設為 disabled
                 // （前端保護，防止自己修改自己）


 ==================================================================================================================
 ============================================================
 [功能說明]
  超級管理員新增帳號，系統自動產生帳號與 Email
  新管理員首次登入後強制修改密碼才可進入後台
  密碼策略：臨時密碼制（首次登入強制修改）
  帳號策略：系統自動生成（ADM001 格式）
============================================================
------------------------------------------------------------
STEP 1：定義資料傳輸物件 (ISpanShop.Models)
------------------------------------------------------------

[V] 更新 AdminDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 對應資料表：Users JOIN Roles JOIN AdminLevels
    - 內容：
        int       UserId            // Users.Id
        string    Account           // Users.Account
        string    Email             // Users.Email
        int       RoleId            // Users.RoleId
        string    RoleName          // Roles.RoleName
        int?      AdminLevelId      // Users.AdminLevelId
        string?   AdminLevelName    // AdminLevels.LevelName
        bool      IsBlacklisted     // Users.IsBlacklisted
        bool      IsFirstLogin      // Users.IsFirstLogin
        DateTime? CreatedAt         // Users.CreatedAt

[V] 新增 AdminLevelDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        int    AdminLevelId    // AdminLevels.Id
        string LevelName       // AdminLevels.LevelName
        string Description     // AdminLevels.Description
        （用於 Modal 下拉選單來源）

[V] 新增 AdminCreateDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        string  Password       // 初始密碼（超管輸入，Service 層負責 Hash）
        int     AdminLevelId   // 選擇管理員等級（只能選 Id=2之後）
        （Account / Email / RoleId 皆由 Service 層自動處理）

[V] 新增 AdminChangePasswordDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        int     UserId
        string  NewPassword       // 新密碼（明文，Service 層負責 Hash）
        string  ConfirmPassword   // 確認新密碼

------------------------------------------------------------
STEP 2：資料存取層 (ISpanShop.Repositories)
------------------------------------------------------------

[ ] 更新 IAdminRepository
    - 位置：ISpanShop.Repositories/Interfaces/IAdminRepository.cs
    - 方法：
        IEnumerable<AdminDto>      GetAllAdmins()
        IEnumerable<AdminLevelDto> GetSelectableAdminLevels()
        AdminDto?                  GetAdminByAccount(string account)
        int                        GetNextAdminSequence()
        bool                       CreateAdmin(string account, string email,
                                               string passwordHash, int roleId,
                                               int adminLevelId)
        bool                       DeactivateAdmin(int userId)
        bool                       IsAccountExists(string account)
        bool                       ChangePassword(int userId, string newPasswordHash)
        bool                       SetFirstLoginComplete(int userId)

[v] 實作 AdminRepository（ADO.NET）
    - 位置：ISpanShop.Repositories/AdminRepository.cs

    - GetAllAdmins：
       新增public string ConfirmPassword隱藏欄位[JsonIgnore]
    - GetSelectableAdminLevels
      （下拉選單來源，排除超級管理員）：
    - GetAdminByAccount： 
    - SQL GetNextAdminSequence：
        SELECT ISNULL(MAX(Id), 0) + 1 FROM Users
    - SQL CreateAdmin：   
    - SQL DeactivateAdmin（軟刪除）：       
    - SQL IsAccountExists：       
    - SQL ChangePassword：     
    - SQL SetFirstLoginComplete：   

------------------------------------------------------------
STEP 3：商務邏輯層 (ISpanShop.Services)
------------------------------------------------------------

[ ] 更新 IAdminService
    - 位置：ISpanShop.Services/Interfaces/IAdminService.cs
    - 方法：
        IEnumerable<AdminDto>      GetAllAdmins()
        IEnumerable<AdminLevelDto> GetSelectableAdminLevels()

        (bool IsSuccess, string Message, string GeneratedAccount)
            CreateAdmin(AdminCreateDto dto)

        (bool IsSuccess, string Message)
            DeactivateAdmin(int userId, int currentUserId)

        AdminDto?
            VerifyLogin(string account, string password)

        (bool IsSuccess, string Message)
            ChangePassword(AdminChangePasswordDto dto)

[v] 實作 AdminService
    - 位置：ISpanShop.Services/AdminService.cs

    - CreateAdmin 邏輯：
        1. 確認 AdminLevelId 不為 1（不可直接新增超級管理員）
        2. 取得流水號
           int seq = _adminRepository.GetNextAdminSequence()
        3. 組成帳號與 Email
           string account = $"ADM{seq:D3}"
           string email   = $"{account}@ispan.com"
        4. 確認帳號不重複（IsAccountExists）
        5. 角色固定為 Admin
           int roleId = 1   
        6. Hash 初始密碼
           string passwordHash = BCrypt.HashPassword(dto.Password)
        7. 呼叫 Repository.CreateAdmin(
               account, email, passwordHash, roleId, dto.AdminLevelId)
        8. 回傳 GeneratedAccount 供畫面顯示

    - DeactivateAdmin 邏輯：
        1. userId != currentUserId（不可停用自己）
        2. 若目標是超級管理員，檢查超級管理員數量 > 1
           SELECT COUNT(1) FROM Users U
           JOIN AdminLevels AL ON U.AdminLevelId = AL.Id
           WHERE AL.Id = 1 AND U.IsBlacklisted = 0
        3. 呼叫 Repository.DeactivateAdmin(userId)

    - VerifyLogin 邏輯：
        1. 呼叫 GetAdminByAccount(account)
        2. 帳號不存在 -> 回傳 null
        3. BCrypt.Verify(password, admin.PasswordHash)
        4. 驗證成功 -> 回傳 AdminDto（含 IsFirstLogin）
        5. 驗證失敗 -> 回傳 null

    - ChangePassword 邏輯：
        1. NewPassword == ConfirmPassword
        2. 新密碼至少 8 碼且含英文與數字
        3. Hash 新密碼
        4. 呼叫 Repository.ChangePassword()
        5. 呼叫 Repository.SetFirstLoginComplete()

------------------------------------------------------------
STEP 4：依賴注入設定 (ISpanShop.MVC)
------------------------------------------------------------

[已完成] Program.cs 已註冊：
         - IAdminRepository / AdminRepository
         - IAdminService / AdminService

[V] Program.cs 新增 Cookie 驗證設定
    builder.Services.AddAuthentication("AdminCookieAuth")
        .AddCookie("AdminCookieAuth", options =>
        {
            options.LoginPath = "/Auth/Login";
            options.AccessDeniedPath = "/Auth/Denied";
            options.ExpireTimeSpan = TimeSpan.FromDays(7);
        });
    app.UseAuthentication(); // 必須在 UseAuthorization 之前
    app.UseAuthorization();

[V] 確認 BCrypt 套件已安裝
    dotnet add package BCrypt.Net-Next

------------------------------------------------------------
STEP 5：展示層 (ISpanShop.MVC) - 管理員管理頁面
------------------------------------------------------------

[已完成] AdminIndexVm / Index.cshtml（基本列表架構）

[V] 更新 AdminIndexVm
    - 位置：ISpanShop.MVC/Models/Admins/AdminIndexVm.cs
    - 內容：
        List<AdminDto>       Admins
        AdminCreateVm        CreateForm
        string               GeneratedAccount
        string               Message

[V] 建立 AdminCreateVm
    - 位置：ISpanShop.MVC/Models/Admins/AdminCreateVm.cs
    - 內容：
        string                    Password
        int                       AdminLevelId
        List<AdminLevelDto>       AdminLevelOptions
        （帳號 / Email / RoleId 皆由系統自動處理）

[V] 更新 AdminsController
    - 位置：ISpanShop.MVC/Controllers/AdminsController.cs

    GET Index()
        -> admins         = AdminService.GetAllAdmins()
        -> levelOptions   = AdminService.GetSelectableAdminLevels()
        -> 組成 AdminIndexVm 回傳

    POST CreateAdmin(AdminCreateVm form)
         -> 呼叫 AdminService.CreateAdmin()
         -> 成功：TempData["GeneratedAccount"] = result.GeneratedAccount
                 TempData["Message"] = "帳號建立成功"
         -> RedirectToAction("Index")

    POST DeactivateAdmin(int userId)
         -> 從 Claims 取得 currentUserId
         -> 呼叫 AdminService.DeactivateAdmin()
         -> TempData["Message"] = 結果訊息
         -> RedirectToAction("Index")

[V] 更新 Index.cshtml

     建立成功提示區塊：
        @if (TempData["GeneratedAccount"] != null)
        {
            <div class="bg-yellow-50 border border-yellow-300 p-4 rounded">
                <p>帳號建立成功！</p>
                <p>帳號：<code>@TempData["GeneratedAccount"]</code></p>
                <p>Email：<code>@TempData["GeneratedAccount"]@ispan.com</code></p>
                <p class="text-sm text-gray-500">
                    請將帳號與初始密碼告知該管理員，首次登入需強制修改密碼。
                </p>
            </div>
        }

     新增管理員 Modal（兩個輸入欄位）：
        初始密碼輸入欄
        管理員等級下拉選單（來源：AdminLevelOptions，不含超級管理員）

     列表新增 AdminLevelName 欄位顯示

     停用按鈕：
        每列新增「停用」按鈕（含 JS confirm 確認）
        IsBlacklisted = true 的列顯示灰底並標記「已停用」
        自己那列停用按鈕設為 disabled

------------------------------------------------------------
STEP 6：展示層 (ISpanShop.MVC) - 實作管理員登入功能
------------------------------------------------------------

[V] 建立 AdminLoginVm
    - 位置：ISpanShop.MVC/Models/Auth/AdminLoginVm.cs
    - 內容：
        string Account
        string Password
        string Message

[V] 建立 AdminChangePasswordVm
    - 位置：ISpanShop.MVC/Models/Auth/AdminChangePasswordVm.cs
    - 內容：
        string NewPassword
        string ConfirmPassword
        string Message

[V] 建立 AuthController
    - 位置：ISpanShop.MVC/Controllers/AuthController.cs

    GET  Login()
         -> 顯示登入頁

    POST Login(AdminLoginVm form)
         -> 呼叫 AdminService.VerifyLogin()
         -> 失敗：回傳錯誤訊息至 View
         -> 成功且 IsFirstLogin = true：
              SignInAsync 建立暫時 Claims
              RedirectToAction("ChangePassword")
         -> 成功且 IsFirstLogin = false：
              SignInAsync 建立完整 Claims
              （含 UserId, Account, RoleName, AdminLevelId）
              RedirectToAction("Index", "Admins")

    GET  ChangePassword()
         -> [Authorize] 需已登入
         -> 顯示修改密碼頁

    POST ChangePassword(AdminChangePasswordVm form)
         -> 從 Claims 取得 UserId
         -> 呼叫 AdminService.ChangePassword()
         -> 成功：RedirectToAction("Index", "Admins")
         -> 失敗：回傳錯誤訊息至 View

    GET  Logout()
         -> SignOutAsync
         -> RedirectToAction("Login")

[V] 建立 Views/Auth/Login.cshtml
    - 帳號輸入欄（格式提示：ADM001）
    - 密碼輸入欄
    - 登入按鈕
    - 錯誤訊息顯示區

[V] 建立 Views/Auth/ChangePassword.cshtml
    - 提示文字：「首次登入請修改密碼」
    - 新密碼輸入欄
    - 確認新密碼輸入欄
    - 確認按鈕
    - 錯誤訊息顯示區
    - 注意：不提供「跳過」或「返回」按鈕

[V] 移除測試用方法並開啟正式驗證
    - 刪除 AdminsController 內的 SetSuperAdminCookie()
    - 刪除 AdminsController 內的 ClearSuperAdminCookie()
    - 取消 [Authorize(Roles = "SuperAdmin")] 的註解

============================================================
  管理員權限頁面 -新增身分與員工帳號
  架構決策：
  - 權限由 AdminLevelPermissions 統一管理
  - 不建 AdminPermissions（個別微調需新增身分達成）
  - IsSystem 不加欄位，Service 層以 AdminLevelId = 1 判斷
  - 帳號名稱手動輸入
============================================================
------------------------------------------------------------
STEP 1：更新資料傳輸物件 (ISpanShop.Models)
------------------------------------------------------------

[V] 更新 AdminDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        int                     UserId
        string                  Account
        int?                    AdminLevelId
        string?                 AdminLevelName
        bool                    IsBlacklisted
        bool                    IsFirstLogin
        DateTime?               CreatedAt
        List<PermissionDto>     ActualPermissions
        // 來源：AdminLevelPermissions（依 AdminLevelId 查詢）

[V] 新增 PermissionDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        int    PermissionId
        string PermissionKey
        string DisplayName
        string Description

[V] 更新 AdminLevelDto
    - 內容：
        int                     AdminLevelId
        string                  LevelName
        string                  Description
        List<PermissionDto>     DefaultPermissions
        // 來源：AdminLevelPermissions

[v] 更新 AdminCreateDto
    - 內容：
        string  Account         // 手動輸入（必填）
        string  Password        // 初始密碼（必填）
        int     AdminLevelId    // 指派身分
        // Email 由 Service 自動組成 Account@ispan.com
        // RoleId 固定為 1（Admin）
        // 無 PermissionIds（權限跟身分走）

[V] 新增 AdminUpdateDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 用途：編輯管理員身分
    - 內容：
        int  UserId
        int  AdminLevelId       // 只能改身分

[V] 新增 AdminLevelCreateDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        string       LevelName
        string       Description
        List<int>    PermissionIds

[V] 新增 AdminLevelUpdateDto
    - 位置：ISpanShop.Models/DTOs/AdminDtos.cs
    - 內容：
        int          AdminLevelId
        string       LevelName
        string       Description
        List<int>    PermissionIds

------------------------------------------------------------
STEP 2：更新資料存取層 (ISpanShop.Repositories)
------------------------------------------------------------

[V] 更新 IAdminRepository
    - 位置：ISpanShop.Repositories/Interfaces/IAdminRepository.cs

    // 已有方法（確認保留）
    IEnumerable<AdminDto>  GetAllAdmins()
    AdminDto?              GetAdminByAccount(string account)
    bool                   IsAccountExists(string account)
    bool                   CreateAdmin(...)
    bool                   DeactivateAdmin(int userId)
    bool                   ChangePassword(int userId, string hash)
    bool                   SetFirstLoginComplete(int userId)

    // 新增方法
    IEnumerable<PermissionDto>     GetPermissionsByAdminLevel(int adminLevelId)
    IEnumerable<AdminLevelDto>     GetAllAdminLevels()
    bool                           UpdateAdminLevel(int userId, int adminLevelId)
    bool                           CreateAdminLevel(AdminLevelCreateDto dto)
    bool                           UpdateAdminLevelConfig(AdminLevelUpdateDto dto)
    bool                           DeleteAdminLevel(int adminLevelId)
    bool                           HasAdminsBoundToLevel(int adminLevelId)
    IEnumerable<PermissionDto>     GetAllPermissions()

[V] 實作新增方法（ADO.NET）
    - 位置：ISpanShop.Repositories/AdminRepository.cs
    - SQL GetPermissionsByAdminLevel：
     - SQL GetAllAdminLevels：
    - SQL UpdateAdminLevel（更新帳號身分）：
    - SQL CreateAdminLevel：
    - SQL UpdateAdminLevelConfig（覆蓋式更新）：
    - SQL DeleteAdminLevel：
     - SQL HasAdminsBoundToLevel：
    - SQL GetAllPermissions：
    - SQL CreateAdmin（修改：Account 改為帶入參數）：
       
------------------------------------------------------------
STEP 3：更新商務邏輯層 (ISpanShop.Services)
------------------------------------------------------------

[V] 更新 IAdminService
    - 位置：ISpanShop.Services/Interfaces/IAdminService.cs

    IEnumerable<AdminDto>      GetAllAdmins()
    IEnumerable<AdminLevelDto> GetAllAdminLevels()
    IEnumerable<PermissionDto> GetAllPermissions()

    (bool IsSuccess, string Message)
        CreateAdmin(AdminCreateDto dto)

    (bool IsSuccess, string Message)
        UpdateAdmin(AdminUpdateDto dto)

    (bool IsSuccess, string Message)
        DeactivateAdmin(int userId, int currentUserId)

    (bool IsSuccess, string Message)
        CreateAdminLevel(AdminLevelCreateDto dto)

    (bool IsSuccess, string Message)
        UpdateAdminLevelConfig(AdminLevelUpdateDto dto)

    (bool IsSuccess, string Message)
        DeleteAdminLevel(int adminLevelId)

[V] 實作 AdminService

    - GetAllAdmins 邏輯：
        1. 呼叫 Repository.GetAllAdmins()
        2. 對每筆 Admin 呼叫
           GetPermissionsByAdminLevel(admin.AdminLevelId)
           補入 ActualPermissions

    - GetAllAdminLevels 邏輯：
        1. 呼叫 Repository.GetAllAdminLevels()
        2. 對每個 Level 呼叫
           GetPermissionsByAdminLevel(level.AdminLevelId)
           補入 DefaultPermissions

    - CreateAdmin 邏輯：
        1. 確認 Account 不重複（IsAccountExists）
        2. 確認 AdminLevelId != 1（不可指派超級管理員）
        3. Email = Account + "@ispan.com"
        4. Hash 密碼
        5. 呼叫 Repository.CreateAdmin()

    - UpdateAdmin 邏輯：
        1. 確認 AdminLevelId != 1（不可升級為超級管理員）
        2. 呼叫 Repository.UpdateAdminLevel()

    - CreateAdminLevel 邏輯：
        1. LevelName 不可為空
        2. 呼叫 Repository.CreateAdminLevel()

    - UpdateAdminLevelConfig 邏輯：
        1. AdminLevelId == 1 -> 拒絕，回傳「系統預設身分不可修改」
        2. 呼叫 Repository.UpdateAdminLevelConfig()

    - DeleteAdminLevel 邏輯：
        1. AdminLevelId == 1 -> 拒絕，回傳「系統預設身分不可刪除」
        2. HasAdminsBoundToLevel() 為 true
           -> 拒絕，回傳「尚有管理員綁定此身分，請先解除綁定」
        3. 呼叫 Repository.DeleteAdminLevel()

------------------------------------------------------------
STEP 4：依賴注入（無需異動）
------------------------------------------------------------

[V] IAdminRepository / AdminRepository 已註冊
[V] IAdminService / AdminService 已註冊

------------------------------------------------------------
STEP 5：更新展示層 (ISpanShop.MVC)
------------------------------------------------------------

[V] 更新 AdminIndexVm
    - 位置：ISpanShop.MVC/Models/Admins/AdminIndexVm.cs
    - 內容：
        List<AdminDto>         Admins
        List<AdminLevelDto>    AdminLevels
        List<PermissionDto>    AllPermissions   // Checkbox 來源
        AdminCreateVm          CreateForm
        AdminLevelCreateVm     LevelCreateForm
        string                 Message
        string                 ActiveTab        // "tab1" or "tab2"

[V] 更新 AdminCreateVm
    - 位置：ISpanShop.MVC/Models/Admins/AdminCreateVm.cs
    - 內容：
        string                 Account          // 手動輸入
        string                 Password
        int                    AdminLevelId
        List<AdminLevelDto>    AdminLevelOptions
        // 無 PermissionIds（權限跟身分走，不可微調）

[V] 新增 AdminLevelCreateVm
    - 位置：ISpanShop.MVC/Models/Admins/AdminLevelCreateVm.cs
    - 內容：
        string       LevelName
        string       Description
        List<int>    PermissionIds

[V] 更新 AdminsController
    - 位置：ISpanShop.MVC/Areas/Admin/Controllers/Identities/AdminController.cs

[V] 重寫 Index.cshtml
    - 位置：ISpanShop.MVC/Areas/Admin/Views/Admin/Index.cshtml

     頁面標題：「後台權限與帳號管理」
     右上角：「+ 新增管理員」按鈕

   [V] 移除底部「權限等級說明」區塊
    - 原本顯示：讀取 / 寫入 / 刪除 / 完整 的 CRUD 圖例
    - 原因：與實際 DB 的功能模組權限不符，具誤導性
    - 動作：直接刪除該 <div> 區塊
   [V] 移除列表中「角色標識」欄位
    - 原本顯示：ADMIN / MEMBER 的 Badge
    - 原因：在管理員專屬頁面中無實質意義且佔用空間
    - 動作：刪除該欄位的 <th> 與對應的 <td>

    [V] 移除列表中 Member 資料列
        - 原本：Roles 資料表的 Admin + Member 都顯示
        - 修改後：Tab1 只顯示 Users（RoleId=1 的管理員帳號）
                  Tab2 只顯示 AdminLevels（不含 Roles 資料）

    ── Tab 切換列 ──────────────────────────────
    [管理員帳號列表]  [身分與權限設定]
    （JS 控制顯示/隱藏，從 Model.ActiveTab 決定預設）

    ── Tab 1：管理員帳號列表 ────────────────────

    欄位：ID、管理員帳號、身分、實際擁有權限(Tags)、操作

     權限 Tags 顯示：
        @foreach (var p in admin.ActualPermissions)
        {
            <span class="tag">@p.DisplayName</span>
        }
        若 ActualPermissions.Count == 0：顯示「無任何權限」

     操作欄：編輯（✏️）、停用（🗑️）

     新增管理員 Modal：
        管理員 ID：唯讀（顯示下一個可用 Id）
        登入帳號：文字輸入（非必填）
        登入密碼：文字輸入（必填）
        確認密碼：文字輸入（必填）
        指派身分：下拉選單（不含超級管理員選項）

         JS 連動：選身分時顯示對應預設權限（唯讀預覽）
          const levelPermissions = @Html.Raw(Json.Serialize(
              Model.AdminLevels.ToDictionary(
                  l => l.AdminLevelId,
                  l => l.DefaultPermissions.Select(p => p.DisplayName)
              )))

          document.getElementById('AdminLevelId')
            .addEventListener('change', function () {
              const names = levelPermissions[this.value] || [];
              document.getElementById('PermissionPreview').innerHTML
                = names.map(n =>
                    `<span class="tag">${n}</span>`).join('');
            });

     編輯管理員 Modal：
        ID：唯讀
        帳號：唯讀（鎖定）
        指派身分：下拉選單（可修改，不含超級管理員）
        密碼：隱藏

     停用確認：JS confirm 二次確認

    ── Tab 2：身分與權限設定 ────────────────────

    欄位：ID、身分名稱、描述、預設功能權限(Tags)、操作

     超級管理員列（AdminLevelId = 1）：
        名稱旁顯示「系統預設」標籤
        編輯 / 刪除按鈕 disabled

     新增身分 Modal：
        身分名稱：必填
        描述：選填
        預設權限：Checkbox 群組（來源：AllPermissions）

     編輯身分 Modal：
        身分名稱：可修改
        描述：可修改
        預設權限：Checkbox 群組（預先勾選現有設定）

     刪除：JS confirm 確認
        失敗時 Toast 顯示「尚有管理員綁定此身分」提示

    ── 共用 ─────────────────────────────────────

     Toast 提示：從 TempData["Message"] 觸發
     防呆：必填欄位未填阻擋送出並高亮錯誤欄位

------------------------------------------------------------
注意事項
------------------------------------------------------------
  * 權限以 AdminLevelPermissions 為準，無個別微調
  * 個別客製需求 -> 新增一個專屬身分來達成
  * AdminLevelId = 1（超級管理員）在 Service 層直接擋
    不依賴資料庫欄位判斷
  * 新增帳號時 AdminLevelOptions 排除 AdminLevelId = 1
  * 修改身分預設權限不溯及既往
  * Email 自動組成：Account + "@ispan.com"

============================================================

   【歷史登入紀錄管理】
[V] 1. 建立 DTO
    位置：ISpanShop.Models 專案 -> DTOs 資料夾
    - 資料傳輸層 (ISpanShop.Models)
    目的：定義資料庫模型與各層級傳遞用的資料結構。
    - [V] 建立 LoginHistoryDto.cs，加入 UserAccount 欄位。

[V] 2. 建立 Repository
    位置：ISpanShop.Repositories 專案 -> Interfaces 與根目錄
    - 資料存取層 (ISpanShop.Repositories)
    目的：直接與資料庫對話，負責寫 LINQ 指令。
    實作細項：
    - [V] 建立介面 ILoginHistoryRepository.cs。
    - [V] 實作 LoginHistoryRepository.cs，使用 EF Core (Include/Join) 取得關聯的使用者帳號。

[V] 3. 建立 Service
    位置：ISpanShop.Services 專案 -> Interfaces 與根目錄
    - 商務邏輯層 (ISpanShop.Services)
    目的：處理資料轉換 (Entity -> DTO) 與商業規則。
    實作細項：
    - [V] 建立介面 ILoginHistoryService.cs。
    - [V] 實作 LoginHistoryService.cs，注入 Repository，取得資料後回傳。

[V] 4. 建立 MVC ViewModel
    位置：ISpanShop.MVC 專案 -> Models/LoginHistories 資料夾
    - 網頁展示層 (ISpanShop.MVC)
    目的：將 DTO 封裝進 Vm，準備給 View 渲染畫面。
    實作細項：
    - [V] 建立 LoginHistoryIndexVm.cs，加入畫面顯示用的格式化欄位 (如時間字串、狀態文字轉換)。

[V] 5. 建立 MVC Controller 與 View
    位置：ISpanShop.MVC 專案 -> Controllers 與 Views/LoginHistory 資料夾
    - 網頁展示層 (ISpanShop.MVC)
    目的：接收請求，調用 Service 並渲染最終 HTML 介面。
    實作細項：
    - [V] 建立 Controller (LoginHistoryController.cs)，調用 Service 並返回視圖。
    - [V] 建立 View (Index.cshtml)，使用 Bootstrap 5 繪製基本的資料表格。

[V ] 6. 加入進階列表功能 (搜尋、排序與分頁)
    位置：跨層級擴充 (Models, Repositories, Services, MVC)
    - 全端功能擴充
    目的：實作動態查詢、分頁邏輯與前端 UI 互動狀態保持。
    實作細項：
    - [V] (DTO) 建立 LoginHistoryCriteria.cs，定義搜尋條件 (Keyword, IsSuccessful, SortColumn, IsAscending, PageNumber, PageSize)。
    - [V] (Repository) 擴充查詢邏輯，動態套用 .Where, .OrderBy, .Skip().Take()，並回傳總筆數。
    - [V] (Service) 擴充邏輯，傳遞 Criteria 給 Repository 並包裝分頁結果給上層。
    - [V] (ViewModel) 擴充 Vm，加入分頁資訊 (總頁數、目前頁碼、總筆數) 及保留目前搜尋與排序狀態。
    - [V] (View) 擴充搜尋區塊：建立 <form> 表單，包含文字搜尋框與 Checkbox 狀態篩選。
    - [V] (View) 擴充下拉式分頁：在表格外側左上角，加入每頁筆數 <select> 切換。
    - [V] (View) 擴充表頭點擊排序：將表格 <th> 改為可點擊連結，支援升降冪切換與箭頭圖示顯示。
    - [V] (View) 擴充底部導覽分頁：在表格外側右下角，加入 Bootstrap 分頁按鈕元件，點擊時帶上目前的搜尋與排序狀態。

  【Table 重建 記錄錯誤帳號的登入】 
  [V] 1. 資料模型與 DTO 層：
       * 更新 LoginHistory.cs (EfModels) 以符合新欄位：UserId (nullable), AttemptedAccount, IPAddress, IsSuccess。
       * 更新 LoginHistoryDto 結構，增加 AttemptedAccount 欄位並同步型別。


   [V] 2. Repository 層 (LoginHistoryRepository)：
       * 移除關聯查詢：由於現在改為紀錄 AttemptedAccount（嘗試帳號），查詢時不再強迫 JOIN Users
         表，這解決了「帳號不存在就查不到紀錄」的問題。
       * 更新欄位映射：所有 CRUD 邏輯均已對應至新欄位名稱（如 IsSuccessful -> IsSuccess）。
       * 強化排序與搜尋：關鍵字搜尋現在會同時比對 AttemptedAccount 與 IPAddress。


   [V] 3. Service 層 (AdminService)：
       * 實現「不存在帳號」紀錄：在 VerifyLogin 中，若 GetAdminByAccount 回傳 null，現在會主動調用 Repository 紀錄一筆
         UserId 為 null 但有 AttemptedAccount 的失敗紀錄。
       * 同步新架構：更新了成功登入時的紀錄邏輯。


   [V] 4. Controller 與展示層：
       * LoginLogsController：移除不再需要的 .Include(l => l.User) 並修正 View 用的對應欄位。
       * LoginHistoryIndexVm：更新 ViewModel 的轉換擴充方法，將 AttemptedAccount 作為顯示主體。
       * LoginHistory/Index.cshtml：修正 View 上的排序連結與狀態顯示邏輯，解決因 IsSuccessful 欄位移除導致的編譯錯誤。


=====================================================================================================================

【後台管理系統 - 管理員權限頁面優化】 

 【STEP 1】DTO 新增
[V] 新增 AdminResetPasswordDto
    - 位置：ISpanShop.Models/DTOs/Admins/AdminDtos.cs
    - 欄位：
        int    UserId
        string NewPassword   // 管理員設定的臨時密碼

【STEP 2】Repository 新增
[V] IAdminRepository 新增介面方法
    - 位置：ISpanShop.Repositories/Admins/IAdminRepository.cs
    - 新增：
        bool ResetPassword(int userId, string passwordHash)
        // 更新密碼 + 將 IsFirstLogin = 1（讓員工下次登入強制改密碼）
 
[V] AdminRepository 實作
    - 位置：ISpanShop.Repositories/Admins/AdminRepository.cs
    - SQL：
        UPDATE Users
        SET Password = @PasswordHash,
            IsFirstLogin = 1,
            UpdatedAt = GETDATE()
        WHERE Id = @UserId
 

【STEP 3】Service 新增
[V] IAdminService 新增介面方法
    - 位置：ISpanShop.Services/Admins/IAdminService.cs
    - 新增：
        (bool IsSuccess, string Message) ResetAdminPassword(AdminResetPasswordDto dto)
 
[v] AdminService 實作
    - 邏輯：
        1. 密碼不可為空
        2. 長度至少 8 碼
        3. BCrypt Hash 新密碼
        4. 呼叫 Repository.ResetPassword()
 
【STEP 4】Controller 更新
[V] AdminController 新增 Action
    - 位置：Areas/Admin/Controllers/Admin/AdminController.cs
    - 新增：
        POST ResetPassword(AdminResetPasswordDto dto)
            -> AdminService.ResetAdminPassword()
            -> TempData["Message"] = 結果訊息
            -> TempData["ActiveTab"] = "tab1"
            -> RedirectToAction("Index")
 
[V] AdminController 加上權限檢查
    - 在 Index() 開頭加：
        var levelId = User.FindFirst("AdminLevelId")?.Value;
        if (levelId != "1")
            return RedirectToAction("Dashboard", "Orders", new { area = "Admin" });
    - 所有 POST Action 也加相同檢查
 
【STEP 5】View 更新
[V] Index.cshtml 新增編輯側拉 Panel
    - 位置：Areas/Admin/Views/Admin/Index.cshtml
    - Tab1 表格每列操作欄加上「編輯」按鈕
    - 側拉 Panel 顯示欄位：
        帳號（唯讀）
        電子信箱（唯讀）
        身分等級（下拉選單，可修改）
        狀態（啟用/停用 Toggle）
        重設臨時密碼（輸入欄 + 送出按鈕）
    - 規則：
        超級管理員（AdminLevelId = 1）的身分等級和狀態不可修改
        不可修改自己的狀態
        重設密碼送出後 IsFirstLogin = 1，員工下次登入強制改密碼

【STEP 6】選單權限控制
[V] _Layout.cshtml 隱藏「管理員管理」選單
    - 只有 AdminLevelId = 1 才顯示「管理員管理」選單項目
    - 修改方式：
        @if (User.FindFirst("AdminLevelId")?.Value == "1")
        {
            // 管理員管理選單 li
        }

============================================================
  店家管理 (Store Management) - 實作 Todolist
  參考：會員管理頁面邏輯
  資料來源：Stores + Products + Users
  架構：Areas/Admin
============================================================
------------------------------------------------------------
STEP 1：定義資料傳輸物件 (ISpanShop.Models)
------------------------------------------------------------

[V] 新增 StoreDto
    - 位置：ISpanShop.Models/DTOs/Stores/StoreDtos.cs
    - 對應資料表：Stores JOIN Users
    - 內容：
        int       StoreId
        int       UserId
        string    OwnerAccount       // Users.Account（店主帳號）
        string    StoreName          // Stores.StoreName
        string?   Description        // Stores.Description
        bool      IsVerified         // Stores.IsVerified
        bool      IsBlacklisted      // Users.IsBlacklisted
        DateTime? CreatedAt          // Stores.CreatedAt
        int       ProductCount       // 商品數量（另查）

[V] 新增 StoreDetailDto
    - 位置：ISpanShop.Models/DTOs/Stores/StoreDtos.cs（同檔）
    - 用途：_EditPartial 顯示詳細資訊
    - 內容：
        int       StoreId
        int       UserId
        string    OwnerAccount
        string    StoreName
        string?   Description
        bool      IsVerified
        bool      IsBlacklisted
        DateTime? CreatedAt
        int       ProductCount        // 商品總數（IsDeleted = 0）
        int       ActiveProductCount  // 上架中商品數（Status = 1）
        int       TotalSales          // 所有商品 TotalSales 加總

------------------------------------------------------------
STEP 2：資料存取層 (ISpanShop.Repositories)
------------------------------------------------------------

[V 新增 IStoreRepository
    - 位置：ISpanShop.Repositories/Stores/IStoreRepository.cs
    - 方法：
        IEnumerable<StoreDto>  GetAllStores()
        StoreDetailDto?        GetStoreById(int storeId)
        bool                   ToggleVerified(int storeId, bool isVerified)
        bool                   ToggleBlacklist(int userId, bool isBlacklisted)

[V] 實作 StoreRepository（ADO.NET）
    - 位置：ISpanShop.Repositories/Stores/StoreRepository.cs

    - SQL GetAllStores：
        SELECT S.Id            AS StoreId,
               S.UserId,
               U.Account       AS OwnerAccount,
               S.StoreName,
               S.Description,
               S.IsVerified,
               U.IsBlacklisted,
               S.CreatedAt,
               COUNT(P.Id)     AS ProductCount
        FROM   Stores S
        JOIN   Users U   ON S.UserId = U.Id
        LEFT JOIN Products P ON P.StoreId = S.Id
                             AND P.IsDeleted = 0
        GROUP BY S.Id, S.UserId, U.Account, S.StoreName,
                 S.Description, S.IsVerified,
                 U.IsBlacklisted, S.CreatedAt
        ORDER BY S.CreatedAt DESC

    - SQL GetStoreById：
        SELECT S.Id              AS StoreId,
               S.UserId,
               U.Account         AS OwnerAccount,
               S.StoreName,
               S.Description,
               S.IsVerified,
               U.IsBlacklisted,
               S.CreatedAt,
               COUNT(P.Id)       AS ProductCount,
               SUM(CASE WHEN P.Status = 1
                        THEN 1 ELSE 0 END)   AS ActiveProductCount,
               ISNULL(SUM(P.TotalSales), 0)  AS TotalSales
        FROM   Stores S
        JOIN   Users U   ON S.UserId = U.Id
        LEFT JOIN Products P ON P.StoreId = S.Id
                             AND P.IsDeleted = 0
        WHERE  S.Id = @StoreId
        GROUP BY S.Id, S.UserId, U.Account, S.StoreName,
                 S.Description, S.IsVerified,
                 U.IsBlacklisted, S.CreatedAt

    - SQL ToggleVerified：
        UPDATE Stores
        SET    IsVerified = @IsVerified
        WHERE  Id = @StoreId

    - SQL ToggleBlacklist：
        UPDATE Users
        SET    IsBlacklisted = @IsBlacklisted,
               UpdatedAt = GETDATE()
        WHERE  Id = @UserId

------------------------------------------------------------
STEP 3：商務邏輯層 (ISpanShop.Services)
------------------------------------------------------------

[V] 新增 IStoreService
    - 位置：ISpanShop.Services/Stores/IStoreService.cs
    - 方法：
        IEnumerable<StoreDto>  GetAllStores()
        StoreDetailDto?        GetStoreById(int storeId)
        (bool IsSuccess, string Message)
            ToggleVerified(int storeId, bool isVerified)
        (bool IsSuccess, string Message)
            ToggleBlacklist(int storeId, bool isBlacklisted)

[V] 實作 StoreService
    - 位置：ISpanShop.Services/Stores/StoreService.cs

    - ToggleVerified 邏輯：
        1. 呼叫 Repository.ToggleVerified()
        2. 回傳結果訊息
           isVerified = true  -> "已通過審核"
           isVerified = false -> "已取消審核"

    - ToggleBlacklist 邏輯：
        1. 呼叫 GetStoreById() 取得 UserId
        2. 若 Store 不存在 -> 回傳 (false, "找不到該店家")
        3. 呼叫 Repository.ToggleBlacklist(userId, isBlacklisted)
        4. 回傳結果訊息
           isBlacklisted = true  -> "已封鎖店家"
           isBlacklisted = false -> "已解除封鎖"

------------------------------------------------------------
STEP 4：依賴注入設定 (ISpanShop.MVC)
------------------------------------------------------------

[V] Program.cs 新增註冊
    builder.Services.AddScoped<IStoreRepository, StoreRepository>();
    builder.Services.AddScoped<IStoreService, StoreService>();

------------------------------------------------------------
STEP 5：展示層 (ISpanShop.MVC)
------------------------------------------------------------

[V ] 建立 StoreIndexVm
    - 位置：ISpanShop.MVC/Areas/Admin/Models/Stores/StoreIndexVm.cs
    - 內容：
        List<StoreDto>  Stores
        string?         Message

[ V] 建立 StoreDetailVm
    - 位置：ISpanShop.MVC/Areas/Admin/Models/Stores/StoreDetailVm.cs
    - 內容：
        StoreDetailDto  Store
        string?         Message

[ V] 建立 StoresController
    - 位置：ISpanShop.MVC/Areas/Admin/Controllers/Stores/StoresController.cs
    - 注意：加上 Area 標記與授權

    [Area("Admin")]
    [Authorize(Roles = "Admin")]
    public class StoresController : Controller
    {
        GET  Index()
             -> StoreService.GetAllStores()
             -> Message = TempData["Message"]
             -> 組成 StoreIndexVm 回傳

        GET  Edit(int storeId)
             -> StoreService.GetStoreById(storeId)
             -> 若找不到 -> RedirectToAction("Index")
             -> 組成 StoreDetailVm
             -> return PartialView("_EditPartial", vm)

        POST ToggleVerified(int storeId, bool isVerified)
             -> StoreService.ToggleVerified()
             -> TempData["Message"] = 結果訊息
             -> RedirectToAction("Index")

        POST ToggleBlacklist(int storeId, bool isBlacklisted)
             -> StoreService.ToggleBlacklist()
             -> TempData["Message"] = 結果訊息
             -> RedirectToAction("Index")
    }

[ V] 建立 Index.cshtml
    - 位置：ISpanShop.MVC/Areas/Admin/Views/Stores/Index.cshtml

     頁面標題：「店家管理」

     TempData 提示區塊：
        @if (Model.Message != null)
        {
            <div class="alert">@Model.Message</div>
        }

     列表欄位：
        店家 ID、店家名稱、店主帳號、商品數、
        審核狀態、封鎖狀態、操作

     審核狀態 Badge：
        IsVerified = true  -> 綠色「已審核」
        IsVerified = false -> 黃色「待審核」

     封鎖狀態 Badge：
        IsBlacklisted = true  -> 紅色「已封鎖」
        IsBlacklisted = false -> 灰色「正常」

     操作欄：
        「查看/編輯」按鈕
        -> 以 AJAX 載入 _EditPartial 至 Modal 中
        -> 範例：
          <button onclick="loadStoreDetail(@store.StoreId)">
              查看/編輯
          </button>

          <div id="storeModal" class="modal">
              <div id="storeModalContent"></div>
          </div>

          <script>
          function loadStoreDetail(storeId) {
              fetch(`/Admin/Stores/Edit/${storeId}`)
                  .then(r => r.text())
                  .then(html => {
                      document.getElementById('storeModalContent')
                              .innerHTML = html;
                      // 開啟 Modal
                  });
          }
          </script>

[V] 建立 _EditPartial.cshtml
    - 位置：ISpanShop.MVC/Areas/Admin/Views/Stores/_EditPartial.cshtml
    - @model StoreDetailVm

     唯讀資訊區塊：
        店家 ID         -> @Model.Store.StoreId
        店家名稱        -> @Model.Store.StoreName
        店主帳號        -> @Model.Store.OwnerAccount
        店家描述        -> @Model.Store.Description
        建立時間        -> @Model.Store.CreatedAt
        商品總數        -> @Model.Store.ProductCount
        上架中商品數    -> @Model.Store.ActiveProductCount
        累計銷售量      -> @Model.Store.TotalSales

     審核狀態切換：
        @if (!Model.Store.IsVerified)
        {
            <form method="post" asp-area="Admin"
                  asp-controller="Stores"
                  asp-action="ToggleVerified">
                <input type="hidden" name="storeId"
                       value="@Model.Store.StoreId" />
                <input type="hidden" name="isVerified" value="true" />
                <button type="submit" class="btn btn-success">
                    通過審核
                </button>
            </form>
        }
        else
        {
            <form method="post" asp-area="Admin"
                  asp-controller="Stores"
                  asp-action="ToggleVerified">
                <input type="hidden" name="storeId"
                       value="@Model.Store.StoreId" />
                <input type="hidden" name="isVerified" value="false" />
                <button type="submit" class="btn btn-secondary">
                    取消審核
                </button>
            </form>
        }

     封鎖狀態切換：
        @if (!Model.Store.IsBlacklisted)
        {
            <form method="post" asp-area="Admin"
                  asp-controller="Stores"
                  asp-action="ToggleBlacklist">
                <input type="hidden" name="storeId"
                       value="@Model.Store.StoreId" />
                <input type="hidden" name="isBlacklisted" value="true" />
                <button type="submit" class="btn btn-danger"
                        onclick="return confirm('確定要封鎖此店家？')">
                    封鎖店家
                </button>
            </form>
        }
        else
        {
            <form method="post" asp-area="Admin"
                  asp-controller="Stores"
                  asp-action="ToggleBlacklist">
                <input type="hidden" name="storeId"
                       value="@Model.Store.StoreId" />
                <input type="hidden" name="isBlacklisted" value="false" />
                <button type="submit" class="btn btn-success">
                    解除封鎖
                </button>
            </form>
        }

     警示文字（封鎖說明）：
        <p class="text-warning text-sm">
            封鎖店家將同步封鎖該店主的會員帳號
        </p>

------------------------------------------------------------
注意事項
------------------------------------------------------------
  * Controller 需標記 [Area("Admin")]，否則路由無法對應
  * _EditPartial 透過 AJAX 載入至 Modal，不做頁面跳轉
  * 封鎖店家 = 更新 Users.IsBlacklisted
    連帶影響：店主的前台會員帳號也會同步被封鎖
    若需獨立控制，可未來在 Stores 資料表新增 IsActive 欄位
  * ProductCount 只計算 IsDeleted = 0 的商品
  * Products.Status = 1 假設為上架中
    實作前請確認：SELECT DISTINCT Status FROM Products

============================================================

============================================================
  管理員權限切版 (Admin Permission-Based UI) - 實作 Todolist
  功能：依登入身分動態顯示選單與保護路由
  資料來源：AdminLevels + Permissions + AdminLevelPermissions
  架構：Areas/Admin + ISpanShop.Common
============================================================
 
------------------------------------------------------------
STEP 1：建立 Permission Helper (ISpanShop.Common)
------------------------------------------------------------
 
[ ] 新增 ClaimsPrincipalExtensions
    - 位置：ISpanShop.Common/Helpers/ClaimsPrincipalExtensions.cs
    - 用途：讓 View 與 Controller 可呼叫 User.HasPermission(key)
    - 內容：
 
        using System.Security.Claims;
 
        public static class ClaimsPrincipalExtensions
        {
            public static bool HasPermission(
                this ClaimsPrincipal user, string permissionKey)
            {
                return user.Claims.Any(c =>
                    c.Type == "Permission" &&
                    c.Value == permissionKey);
            }
 
            public static bool IsSuperAdmin(this ClaimsPrincipal user)
            {
                return user.Claims.Any(c =>
                    c.Type == "AdminLevelId" &&
                    c.Value == "1");
            }
        }
 
------------------------------------------------------------
STEP 2：定義資料傳輸物件 (ISpanShop.Models)
------------------------------------------------------------
 
[V] 確認 / 新增 AdminPermissionDto
    - 位置：ISpanShop.Models/DTOs/Admins/AdminPermissionDto.cs
    - 用途：登入後從 DB 查詢該管理員所有權限
    - 內容：
        int     AdminId
        int     LevelId
        string  LevelName
        List<string> PermissionKeys   // 所有擁有的 PermissionKey 清單
 
------------------------------------------------------------
STEP 3：資料存取層 (ISpanShop.Repositories)
------------------------------------------------------------
 
[V] 確認 / 新增 IAdminRepository（新增方法）
    - 位置：ISpanShop.Repositories/Admins/IAdminRepository.cs
    - 新增方法：
        AdminPermissionDto? GetAdminWithPermissions(int adminId)
 
[V] 實作 GetAdminWithPermissions（ADO.NET）
    - 位置：ISpanShop.Repositories/Admins/AdminRepository.cs
 
    - SQL：
        SELECT A.Id          AS AdminId,
               A.LevelId,
               AL.LevelName,
               P.PermissionKey
        FROM   Admins A
        JOIN   AdminLevels AL
                   ON A.LevelId = AL.Id
        JOIN   AdminLevelPermissions ALP
                   ON AL.Id = ALP.AdminLevelId
        JOIN   Permissions P
                   ON ALP.PermissionId = P.Id
        WHERE  A.Id = @AdminId
 
    - 組裝邏輯：
        1. 讀取第一筆取得 AdminId / LevelId / LevelName
        2. 迴圈累加所有 PermissionKey 至 List<string>
        3. 回傳 AdminPermissionDto
 
------------------------------------------------------------
STEP 4：登入時寫入 Claims (ISpanShop.MVC)
------------------------------------------------------------
 
[V] 修改登入 Action（AuthController 或 AccountController）
    - 位置：ISpanShop.MVC/Areas/Admin/Controllers/Auth/...
    - 登入成功後執行：
 
        // 1. 查詢該管理員的所有權限
        var adminPerm = _adminRepository
                            .GetAdminWithPermissions(admin.Id);
 
        // 2. 組裝 Claims
        var claims = new List<Claim>
        {
            new Claim(ClaimTypes.Name,   admin.Account),
            new Claim(ClaimTypes.NameIdentifier,
                                         admin.Id.ToString()),
            new Claim("AdminLevelId",    admin.LevelId.ToString()),
            new Claim("LevelName",       adminPerm.LevelName),
        };
 
        // 3. 將每個 PermissionKey 加入 Claims
        foreach (var key in adminPerm.PermissionKeys)
        {
            claims.Add(new Claim("Permission", key));
        }
 
        // 4. 建立 Cookie（原有邏輯維持，替換 claims 來源）
        var identity  = new ClaimsIdentity(claims,
                            CookieAuthenticationDefaults
                                .AuthenticationScheme);
        var principal = new ClaimsPrincipal(identity);
        await HttpContext.SignInAsync(principal);
 
    - 注意事項：
        * 若原本已有登入邏輯，只需在 SignInAsync 前
          替換 claims 組裝段落即可
        * LevelId == 1 為超級管理員，
          IsSuperAdmin() helper 會使用此值
 
------------------------------------------------------------
STEP 5：建立 RequirePermission Attribute (ISpanShop.MVC)
------------------------------------------------------------
 
[V] 新增 RequirePermissionAttribute
    - 位置：ISpanShop.MVC/Middleware/RequirePermissionAttribute.cs
    - 內容：
 
        using ISpanShop.Common.Helpers;
 
        public class RequirePermissionAttribute
            : Attribute, IAuthorizationFilter
        {
            private readonly string _permissionKey;
 
            public RequirePermissionAttribute(string permissionKey)
            {
                _permissionKey = permissionKey;
            }
 
            public void OnAuthorization(
                AuthorizationFilterContext context)
            {
                var user = context.HttpContext.User;
 
                // 未登入 -> 導向登入頁
                if (!user.Identity.IsAuthenticated)
                {
                    context.Result = new RedirectToActionResult(
                        "Login", "Auth", new { area = "Admin" });
                    return;
                }
 
                // 無此權限 -> 導向 AccessDenied 頁
                if (!user.HasPermission(_permissionKey))
                {
                    context.Result = new RedirectToActionResult(
                        "AccessDenied", "Auth",
                        new { area = "Admin" });
                }
            }
        }
 
[V] 新增 RequireSuperAdmin Attribute（超級管理員專用）
    - 位置：ISpanShop.MVC/Middleware/RequireSuperAdminAttribute.cs
    - 用途：管理員權限設定、登入紀錄等頁面
    - 內容（同上，改用 IsSuperAdmin() 判斷）
 
[V] 新增 AccessDenied Action
    - 位置：Auth Controller
    - 回傳：403 提示頁面或 View
 
------------------------------------------------------------
STEP 6：各 Controller 套用 Attribute
------------------------------------------------------------
 
[ ] ProductsController（商品監管）
    [RequirePermission("product_manage")]
 
[ ] CategoriesController（分類管理）
    [RequirePermission("product_manage")]
 
[ ] InventoryController（庫存監控）
    [RequirePermission("store_manage")]
 
[ ] OrderManagementController（訂單管理）
    [RequirePermission("cashflow_manage")]
 
[ ] PaymentManagementController（金流管理）
    [RequirePermission("cashflow_manage")]
 
[ ] ContentModerationController（內容與客服）
    - 工單相關 Action：[RequirePermission("ticket_manage")]
    - 敏感詞相關 Action：[RequirePermission("sensitive_manage")]
    - 注意：同一 Controller 可在 Action 層個別套用
 
[ ] MemberController（會員管理）
    [RequirePermission("member_manage")]
 
[ ] SellerController（店家管理）
    [RequirePermission("store_manage")]
 
[ ] LoginHistoryController（登入紀錄）
    [RequireSuperAdmin]  ← 超級管理員限定
 
[ ] AdminPermissionController（管理員權限設定）
    [RequireSuperAdmin]  ← 超級管理員限定
 
[ ] HomeController / DashboardController（首頁儀錶板）
    [RequirePermission("report_view")]
    ※ 或改為：只要登入就能看（[Authorize] 即可）
       視業務需求決定
 
------------------------------------------------------------
STEP 7：選單切版 (Sidebar Layout)
------------------------------------------------------------
 
[ ] 修改 _AdminLayout.cshtml 或 Sidebar Partial
    - 位置：ISpanShop.MVC/Areas/Admin/Views/Shared/
    - 在最上方加入：
        @using ISpanShop.Common.Helpers
 
    - 選單結構（條件顯示）：
 
      ── 首頁 ──────────────────────────────────────
        首頁（儀錶板）：永遠顯示（登入即可）
 
      ── 營運管理 區塊 ────────────────────────────
        @{
            bool showOperations =
                User.HasPermission("product_manage")  ||
                User.HasPermission("store_manage")    ||
                User.HasPermission("cashflow_manage") ||
                User.HasPermission("ticket_manage")   ||
                User.HasPermission("sensitive_manage");
        }
        @if (showOperations)
        {
            <p class="menu-label">營運管理</p>
 
            @if (User.HasPermission("product_manage"))
            {
                商品監管 連結
                分類管理 連結
            }
 
            @if (User.HasPermission("store_manage"))
            {
                庫存監控 連結
            }
 
            @if (User.HasPermission("cashflow_manage"))
            {
                訂單管理 連結
                金流管理 連結
            }
 
            @if (User.HasPermission("ticket_manage") ||
                 User.HasPermission("sensitive_manage"))
            {
                內容與客服 連結
            }
        }
 
      ── 帳號管理 區塊 ────────────────────────────
        @{
            bool showAccount =
                User.HasPermission("member_manage") ||
                User.HasPermission("store_manage")  ||
                User.IsSuperAdmin();
        }
        @if (showAccount)
        {
            <p class="menu-label">帳號管理</p>
 
            @if (User.HasPermission("member_manage"))
            {
                會員管理 連結
            }
 
            @if (User.HasPermission("store_manage"))
            {
                店家管理 連結
            }
 
            @if (User.IsSuperAdmin())
            {
                登入紀錄 連結
                管理員權限 連結
            }
        }
 
------------------------------------------------------------
STEP 8：ContentModeration 頁面內部細分（選做）
------------------------------------------------------------
 
[ ] 修改 ContentModeration Index.cshtml
    - 工單 Tab / 功能區塊：
        @if (User.HasPermission("ticket_manage"))
        {
            顯示客服工單區塊
        }
    - 敏感詞 Tab / 功能區塊：
        @if (User.HasPermission("sensitive_manage"))
        {
            顯示敏感詞管理區塊
        }
 
------------------------------------------------------------
STEP 9：Program.cs 確認設定
------------------------------------------------------------
 
[ ] 確認 Cookie 驗證已正確設定
    - 位置：ISpanShop.MVC/Program.cs
    - 檢查：
 
        builder.Services.AddAuthentication(
            CookieAuthenticationDefaults.AuthenticationScheme)
            .AddCookie(options =>
            {
                options.LoginPath       = "/Admin/Auth/Login";
                options.AccessDeniedPath= "/Admin/Auth/AccessDenied";
                options.ExpireTimeSpan  = TimeSpan.FromHours(8);
            });
 
        // Middleware 順序（必須在 UseAuthorization 之前）
        app.UseAuthentication();
        app.UseAuthorization();
 
------------------------------------------------------------
STEP 10：測試驗證
------------------------------------------------------------
 
[ ] 測試情境 1：超級管理員（LevelId = 1）
    -> 所有選單項目都顯示
    -> 所有頁面均可存取
 
[ ] 測試情境 2：客服工單管理員
    -> 選單只顯示「內容與客服」
    -> 直接輸入 /Admin/Products 網址 -> 導向 AccessDenied
 
[ ] 測試情境 3：財務管理員
    -> 選單只顯示「訂單管理」、「金流管理」
    -> 直接輸入 /Admin/Members 網址 -> 導向 AccessDenied
 
[ ] 測試情境 4：經營分析（report_view only）
    -> 選單只顯示「首頁」
    -> 其他所有頁面均被擋下
 
[ ] 測試情境 5：未登入
    -> 直接輸入任意後台網址 -> 導向 Login
 
------------------------------------------------------------
PermissionKey 對應選單 速查表
------------------------------------------------------------
 
  PermissionKey      | 可見選單項目
  -------------------|------------------------------------
  product_manage     | 商品監管、分類管理
  store_manage       | 庫存監控、店家管理
  cashflow_manage    | 訂單管理、金流管理
  ticket_manage      | 內容與客服（工單功能）
  sensitive_manage   | 內容與客服（敏感詞功能）
  member_manage      | 會員管理
  report_view        | 首頁儀錶板
  超級管理員專用      | 登入紀錄、管理員權限設定
 
------------------------------------------------------------
注意事項
------------------------------------------------------------
 
  * STEP 1~5 為基礎建設，必須先完成才能進行後續步驟
  * STEP 6（Controller Attribute）是安全核心，
    選單隱藏只是 UX，Controller 保護才是真正防線
  * ContentModerationController 若工單與敏感詞在
    同一個 Controller，建議在 Action 層個別套用 Attribute
    而非整個 Controller 套用
  * IsSuperAdmin() 判斷依賴 Claim "AdminLevelId" == "1"，
    登入時務必確認此 Claim 有正確寫入
  * 若未來新增管理員身分，只需在資料庫
    AdminLevelPermissions 新增對應紀錄即可，
    程式碼不需修改
 
============================================================
 